{% set version = "1.0.0beta13" %}

package:
  name: emperor
  version: {{ version }}

source:
  fn: emperor-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/e/emperor/emperor-{{ version }}.tar.gz
  sha256: b5bab8dfd4cff5a6c22a28c21088b196a19ca10dc65dd70a8d7825ab187276be

build:
  number: 1
  skip: true  # [win]
  script: python setup.py install --record=record.txt

requirements:
  build:
    - python
    - scipy >=0.17.0
    - future
    - jinja2
    - scikit-bio >=0.4.1,<0.5.0  # [py2k]
    - scikit-bio >=0.4.1  # [py3k]
    - numpy >=1.7
    - pandas
    - click

  run:
    - python
    - scipy >=0.17.0
    - future
    - jinja2
    - scikit-bio >=0.4.1,<0.5.0  # [py2k]
    - scikit-bio >=0.4.1  # [py3k]
    - numpy >=1.7
    - pandas
    - click

test:
  imports:
    - emperor
    - emperor.qiime_backports

about:
  home: http://emperor.microbio.me
  license: BSD 3-Clause
  summary: 'Emperor a tool for the analysis and visualization of large microbial ecology datasets'

extra:
    recipe-maintainers:
        - ElDeveloper
